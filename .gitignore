node_modules
dist
.wrangler
.dev.vars
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite
coverage
.turbo
.cache
apps/web/dist
apps/web/dist-ssr
apps/web/dist.old.*
apps/api/dist
.claude
apps/web/scripts/seo/output
.playwright-mcp/*
# ── Pipelines de vídeo ────────────────────────────────────────────────────
# Las carpetas de vídeo van enteras fuera del repo: los MP4, capturas y frames
# intermedios son binarios pesados, y el propio pipeline (plantillas, guiones,
# scripts) se mantiene fuera del control de versiones por decisión del proyecto.
tools/
tools2/
# Los .mp4/.webm sueltos, vengan de donde vengan
*.mp4
*.webm
